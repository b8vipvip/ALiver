name: Ruff debug

on:
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: git clone --depth 1 --branch agent/simli-provider-v1 https://github.com/b8vipvip/ALiver.git .
      - run: pipx run ruff check app bridge tests
